---
id: P-008
type: plan
status: active
links: [G-001, AN-011, AN-003, AN-008, AN-010]
title: Cliewen agrees with itself before widening its promises
---

# P-008 — Cliewen agrees with itself before widening its promises

P-007 hardened the core and closed with every milestone evidenced, but [AN-011](../analysis/AN-011-methodology-self-consistency.md) found that several public and agent-facing carriers still describe the evidence model P-007 replaced. A mechanically green corpus whose live instructions disagree cannot satisfy [G-001](../goals/G-001-verifiable-thread.md): the thread is verifiable only when the judge, the skills, and the human-facing explanation state the same contract.

This campaign works from demonstrated contradiction outward. It repairs current truth before adding process, consumes the cross-trial analysis protocol before changing adoption, makes extraction reversible before asking a real adopter to price configuration, and uses findings rather than speculative interfaces for distributed boundaries. Every milestone is a separate full Cliewen change rooted at accepted `main`, published as a ready pull request, and accepted by human merge before the next begins. Milestone numbering continues corpus-global numbering from P-007 (M-032…M-036).

M-032 is deliberately the largest milestone and is not split. Its rule is that a contract change updates every carrier stating that contract in one change, and a repair that leaves the guide, the extraction skill, and the criteria in separate merges would be the first violation of the rule it establishes — an intermediate `main` would state the contract two ways. The reviewer therefore reads one wide but single-subject diff, all of it the same evidence-model claim restated per carrier, rather than several narrow diffs whose intermediate states are each incoherent.

## Milestones

| ID | Milestone (exit criterion) | Status | Evidence |
|---|---|---|---|
| M-032 | **Every live evidence-model carrier states the v0.9 contract**: a PDR establishes that a methodology change inventories and updates every live carrier that states the affected contract in the same change, superseding PDR-013's executable-only core-thread wording, refining C-006 and the shared lifecycle-skill guidance, and satisfying C-013's human-acceptance boundary; the canonical `clue-extract` source allows whole-file draft phasing, per-criterion `@draft` inside an active file, and genuine Human proof without calling the capability the smallest activation unit, while the canonical `clue-verify` source recognizes classified pairs, single-direction, Human proof, and per-criterion `@draft` instead of requiring tests or a draft capability; all `.agents` and scaffold-template copies of both skills are regenerated from those sources; the repository and scaffold pull-request templates state the same classified-pair, single-direction, Human, and `@draft` rule, with their emitted-copy synchronization and content guards; `guide/adoption.md`, `getting-started.md`, `operations.md`, `what-is-cliewen.md`, `design.md`, `methodology.md`, and `change-loop.md`, plus `CONTRIBUTING.md`, CAP-001's `design.md`, and the scaffolded capabilities README, distinguish classified positive/negative evidence, the legacy one-reference rule, single-direction, Human, `@draft`, supported Go/JVM/Cucumber carriers, and the fact that `clue` validates references but does not execute tests; G-001's title and body, its regenerated goals index, ARCH-003, the repository `README.md`, `docs/README.md`, both repository and scaffold `AGENTS.md` routing hubs, the scaffolded corpus README, the CLI usage tagline, plugin-marketplace and guide-site metadata, the guide landing page, and the new PDR make the Human acceptance-brief path part of the verifiable thread without presenting it as executable code evidence; scaffold and content guards cover the corrected core, CLI/plugin/site entry points, Human, classified/single-direction, and per-criterion-draft claims; CAP-001 and CAP-003 criteria plus positive and negative tests reject the stale contract, including a new CAP-003 criterion stating the activation-granularity rule that no current criterion carries; the PDR says plainly that those anchors bind only the claims repaired here and that the general obligation stays agent-enforced, because nothing derives the carrier set for an arbitrary contract; `[Unreleased]` explains the corrected extraction and evidence guidance; `clue validate --forbid-changes`, coverage and reality reports, `go test ./...`, guide build, scaffold and CI-scope tests pass | `todo` | |
| M-033 | **Analysis evidence distinguishes a clean claim from a prepared machine and qualifies population results**: consuming AN-003 and the AN-004/AN-005 cross-trial confirmation, `clue-analysis` requires an investigator to say whether verification used a clean disposable environment or a prepared environment with local prerequisites; a prepared result names those prerequisites and cannot be presented as onboarding reproducibility; statistical or percentage claims name the population, versioned corpus, eligibility rules, exclusions with reasons, sampling or repetition method, uncertainty, and deterministic-versus-quality boundary; adoption analysis names governance changes rather than describing scaffolding as neutral; the lasting workflow decision is explicitly routed, canonical sources regenerate without drift, and generator tests hold each new obligation | `todo` | |
| M-034 | **Extraction rehearses before it mutates**: `clue-extract` begins with a mandatory report-only pass that writes its durable extraction report but changes no target source, corpus, tests, routing, or hosted state; the report inventories source formats and entry points, proposed artifact mappings, preserved and minted IDs, confidence and reversal cost, test-purpose work, instruction conflicts, planned deletions, and named plan doors; unresolved conflicts become open questions and stop before mutation; only explicit human direction continues into the existing full extraction change, and the eventual ready PR still deletes the parallel source corpus; a PDR records the workflow boundary, generated skills and guide state it consistently, and tests fail if the mutate phase can precede the report-only checkpoint | `todo` | |
| M-035 | **A real adopter prices configuration before Cliewen chooses an interface**: on an external adopting repository selected by the human, a pinned analysis reproduces the current CI-wall runner/action/binary assumptions, `.agents` and `.claude` placement constraints, and the need for local skills beside managed skills; it measures the concrete edits or failures required by that repository, separates mandatory needs from preferences, evaluates configuration locations against ADR-013's existing AGENTS-local-layer boundary, and ends with explicit ADR/PDR candidates and a named successor-plan consumer; no configuration file, mirror option, or extension mechanism is implemented in this milestone | `todo` | |
| M-036 | **Distributed work and evidence boundaries are reproduced before they become contracts**: pinned scenarios exercise one authorized dependent change, one capability whose acceptance evidence spans repositories, and one external-tracker reference that must survive a repository move; the analysis records which guarantees current branch/PR, repository-local validation, and stable-ID rules can and cannot supply, rejects any option that weakens the human merge boundary or makes forge state the system-of-record, and ends with independently routable candidates for stacked changes, cross-repository evidence, and tracker metadata plus a named successor-plan consumer; no interface is implemented in this milestone | `todo` | |

## Explicitly out of this campaign

The production feedback loop beyond incident analyses, external constraint-catalog mechanics, and native validation of foreign documentation kinds remain future doors. A mechanical carrier inventory — declaring which carriers state which contract so a validator can flag the ones a change left behind — is a named door and not an M-032 deliverable: [AN-011](../analysis/AN-011-methodology-self-consistency.md) shows the obligation but supplies no evidence for what such a declaration would have to record, and a successor plan should open it only after M-032 and M-034 have exercised the prose rule on real contract changes. P-008 does not change the Intent Engineering book, adopt Cliewen into another repository without its maintainer's authority, or promise the interfaces M-035 and M-036 exist to investigate. Package-manager channels and already-closed P-006 findings remain closed scope.

## Mutation rules

Status and evidence fields in the milestone table may mutate in an implementing change's merge digest. Everything else changes only through a declared plan revision backed by a decision record routed by reversal cost. Plan adjustments are decisions.
