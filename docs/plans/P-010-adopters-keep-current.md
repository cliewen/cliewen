---
id: P-010
type: plan
status: active
links: [P-009, G-001, G-002, AN-007, AN-011, AN-013, ADR-011, ADR-021, ADR-022, ADR-031, ADR-039, ADR-042, PDR-019, PDR-022, C-008, C-013]
title: Cliewen makes staying current something an adopter notices
---

# P-010 — Cliewen makes staying current something an adopter notices

This campaign is [P-009](P-009-migration-readiness.md)'s successor, and it is designated here rather than in P-009's own digest because CH-099 deliberately closed that campaign without naming one and [C-008](../constraints/C-008-completed-plans-immutable.md) freezes it afterwards. The forward link can only live on this side of the boundary.

P-009 gave corpus releases a previewable mechanical upgrade path and closed on it. What it did not give anyone was a reason to run that path, or a place to learn it exists. `clue migrate` is named in an adopted repository — in three of the five skills, in the emitted `CLAUDE.md`, and in the emitted `docs/README.md` — but never as something to go and do: the skills mention it only inside the shared local-conventions paragraph, as a precondition of a change already under way, so an agent asked to bring a repository up to date meets no entry point. The word "upgrade" occurs in none of the five generated skills or their canonical sources, and the emitted routing hub names neither upgrading, migrating, nor versions — and that hub is the adopter's file, which `clue init` never overwrites and `clue migrate` never rewrites, so routing added there reaches new repositories only. Only skills reach an adopter who has already onboarded.

Worse than absent guidance is confident silence. An old binary carrying matching old skills validates green, and `clue migrate` on that pair reports "no changes needed" — a sentence an adopter reasonably reads as "you are current". Nothing in the system distinguishes being up to date from being unable to tell. The one moment a repository does learn something is wrong is the drift message, which names no command that would fix it.

Three backlogs were also routed forward across P-007, P-008, and P-009 without a campaign that owns them, and the judge reports all three by count. When this campaign opened it reported 27 decisions still `inferred` — agent-authored and never human-verified — 13 constraints whose only enforcement is an agent remembering to read them, and four capabilities contradicted by AN-007, AN-011, and AN-013. Those figures are the condition at opening, not a standing claim; the campaign's own progress moves them, and the milestones below are written against what the judge reports rather than against a number fixed here. A methodology whose core promise is that humans verify meaning and machines enforce form cannot leave nearly half its own decisions unverified and all but two of its own constraints unenforced by any machine.

This campaign runs in two arcs. The first makes staying current visible and actionable: a command that reports a newer release and shows the recipe for the machine it is actually running on, routing that reaches existing adopters through a skill rather than through a file Cliewen may not touch, and discovery at the moment a coding agent starts or its context window is restarted. The second clears the three backlogs. Every milestone is a separate full Cliewen change rooted at accepted `main`, published as a ready pull request, and accepted by human merge before the next begins. Milestone numbering continues corpus-global numbering from P-009.

## Milestones

| ID | Milestone (exit criterion) | Status | Evidence |
|---|---|---|---|
| M-045 | **A repository can tell it is behind, and is shown how to catch up on the machine it is running on**: an ADR states why a release check may reach the network while the deterministic judge never may, keeping `clue validate` offline and naming what separates a reporting command from a background updater; a command reports a newer release, names the platform's own installation route — the PowerShell script on Windows, the shell script on macOS and Linux, `go install` where no prebuilt asset exists — and prints the coordinated preview-and-apply sequence, writing no file with or without flags; a quiet mode prints one line when behind and nothing at all otherwise, so it is usable where output is context; every degradation is silent and exit 0, covering offline, timeout, rate limit, and unrecognized response, since a check that complains when the network is down is a check that gets removed; the answer is cached outside the repository with a bounded lifetime so a repeated call costs no request, and an unreadable cache is absence rather than error; the drift message names the command that resolves it and the way to pin instead; the platform branch and the network are both injected so all three recipes and every degradation are proven without a test reaching a live service; operations and quickstart guidance and `[Unreleased]` state the same contract | `done` | CH-107: [ADR-042](../decisions/ADR-042-release-check-outside-the-judge.md) states the boundary and the second one it needs — no repository write, no self-replacement, no verdict. `clue latest [--quiet] [--timeout]` reports both versions, one platform route (PowerShell, shell, or `go install` pinned where no asset exists), and the `clue migrate` preview-and-apply sequence. Offline, timeout, rate limit, and an unrecognized body are one answer at exit 0 with an empty stderr; an unknown answer is never cached, so an outage cannot silence the next call. The answer lives at `<user cache dir>/cliewen/latest-release.json` for 24 hours, and missing, stale, unparseable, future-stamped, and unwritable are all absence. The drift message names `clue latest`, `clue migrate`, and the release to install to stay put. AC-075…AC-079 carry positive and negative Unit evidence with the network, clock, cache directory, and platform all injected, so no test depends on a live service; the routes are additionally bound to `.goreleaser.yaml`'s matrix and the quickstart's published commands, and a tag is accepted only as three decimal numbers with the printed version rebuilt from them. `guide/operations.md`, `guide/getting-started.md`, CAP-004 and its design, and `[Unreleased]` state the same contract. |
| M-046 | **Upgrade routing reaches repositories that have already onboarded**: a migration adds a whole managed skill directory when the remaining carrier set is recognized as a supported release, replacing the finding that today blocks the entire write set whenever a directory has no sibling to identify it, and its preview names the release whose bytes it writes rather than the one it recognized; a generated skill gives the upgrade its own named entry point, instructing an agent to run the release check, read the release notes' migration section, **ask the human whether to upgrade now or later**, and — on yes — make the repository green, branch, move the coordinated set together, resolve every notice including those no command may repair, verify, open a pull request, and never merge it; the skill reproduces no installation command of its own, since one text shipped to every repository cannot name the platform each agent is running on; every decision that fixed the public skill set at five — [ADR-021](../decisions/ADR-021-generated-standalone-skills.md), [ADR-022](../decisions/ADR-022-skill-ownership-marker.md)'s reserved directory slots, and [ADR-031](../decisions/ADR-031-plugin-ships-a-bootstrap-only.md) — is amended or replaced rather than left contradicting the shipped set; every live carrier stating the skill inventory moves in the same change; a repository carrying the previous release's carriers is replayed against the new binary and proves both directions — blocked without the migration, upgraded with it | `done` | CH-108: [ADR-043](../decisions/ADR-043-upgrade-skill-is-a-managed-carrier.md) defines six generated, repository-scoped skills and preserves the human choice and merge boundary. `clue-upgrade` checks the release, reads migration notes, asks now or later, and on yes guides a green, branched, coordinated, verified PR without naming an install command or merging. MIG-003 adds its canonical directory only when every carrier from a preceding released set matches exactly; the preview names the target bytes and recognized release, while a partial set remains a finding. AC-080 and AC-081 provide positive and negative Unit evidence with a replay of v0.12.0; AC-030 is retired and AC-082 now protects all six reserved slots. ADR-021, ADR-022, ADR-031, ADR-039, capabilities, generated and scaffolded guidance, operations, plugin guidance, and `[Unreleased]` state the same contract. |
| M-047 | **A coding agent learns a release is available when it starts or its context is restarted**: a decision record extends the vendor entry-point boundary from a file that only points to configuration that only runs the quiet release check, stating why executable vendor configuration is a distinct category and binding it to the same limits — adopter-owned on arrival, never overwritten by materialization and never rewritten by migration, silent when current or offline, bounded by a timeout, and incapable of failing a session; the scaffold emits that configuration for a new session and for a restarted context window alike; an existing file is reported and left byte-identical; the absence of the check in a file the adopter owns is reported and never repaired; the line is observed arriving in a real session rather than asserted from documentation | `todo` | |
| M-048 | **Cliewen's own decisions are human-verified or retired**: every decision the judge counts as `inferred` is either promoted with its approver, date, and venue recorded, or superseded and deleted with the dead identity named on its successor; the count the judge reports reaches zero or the remainder is a named, reasoned exception rather than a backlog; an objection recorded during the pass becomes an open question and a decision, never a silent skip | `done` | CH-105: all 27 records the judge counted — ADR-023, ADR-025…ADR-040, PDR-013…PDR-022 — are `verified`, signed `Flemming N. Larsen (2026-08-02, conversation)`, with `author: agent` and every body unchanged. One blanket approval, made informed: it was stated first, then the complete list and two consequences were put in front of the human before it was carried forward — the log row records that sequence rather than claiming twenty-seven individual readings. None was superseded; no objection was raised, so no record was held back and no open question opened. `clue validate` reports no inferred decisions, and each file differs by exactly its two frontmatter lines. |
| M-049 | **A constraint is enforced by a machine or is declared unenforceable and why**: every constraint the judge counts as agent-enforced either gains a machine check for its detectable subset or states in its own carrier which part can never leave human or agent judgment and what that costs; the count the judge reports reaches zero or the remainder is explicitly declared; no check is weakened to reach the number | `todo` | |
| M-050 | **The capabilities the analyses contradict agree with reality or say where they do not**: for each capability the judge reports as contradicted, the contradiction is resolved by correcting the capability, narrowing its promise, or recording an accepted limit that the analysis and the capability both state; the reality-gap report reaches zero or names what remains and why | `todo` | |

## Explicitly out of this campaign

**Simplification is deferred to P-011.** It is real work and it is deliberately not folded in here: a campaign that carries both would let "we should simplify first" postpone three backlogs that have already waited through three campaigns.

**The judge never reaches the network.** No milestone puts a network call inside `clue validate` or anywhere a validation verdict can read it. The separation is the point of M-045's decision record, not an implementation detail of it.

**`clue` does not replace its own binary.** The installation scripts verify every download against the release's published checksums and refuse to install on a mismatch; a self-updating binary would have to reimplement that verification inside itself, and a program that can overwrite itself from the network is a different kind of program than an offline judge. The convenience an adopter asked for is delivered by an agent following the skill, under the human merge boundary, not by the command.

**No milestone weakens the human merge boundary.** Nothing here lets an agent upgrade a repository and accept the result, and the automatic discovery in M-047 informs a session; it never acts on its own.

## Mutation rules

Status and evidence fields in the milestone table may mutate in an implementing change's merge digest. Everything else changes only through a declared plan revision backed by a decision record routed by reversal cost. Plan adjustments are decisions.
