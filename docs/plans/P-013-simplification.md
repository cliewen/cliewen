---
id: P-013
type: plan
status: active
links: [P-012, G-001, C-004, C-011, C-013, ADR-013, ADR-017, ADR-021, ADR-040, ADR-046, PDR-013, PDR-019, PDR-025, PDR-026, PDR-028, PDR-029, PDR-030, PDR-031, PDR-032, AN-006, AN-008, AN-010, AN-012, AN-013, AN-018]
title: Cliewen is simplified against a stated criterion
---

# P-013 — Cliewen is simplified against a stated criterion

This campaign is [P-012](P-012-migration-gap-closes-on-evidence.md)'s successor and is open. [PDR-026](../decisions/PDR-026-campaigns-close-on-re-derived-evidence.md) moved simplification here from P-012; [PDR-025](../decisions/PDR-025-brownfield-migration-precedes-simplification.md)'s reason for deferring it in the first place still holds, and this file exists so that the deferral is a named destination rather than a phrase in successive campaigns' out-of-scope sections.

Simplification has been deferred by every campaign that named it, each one to its own successor, three times over. That is worth stating plainly, because a thing deferred three times is either not actually wanted or has never been given a shape it could be executed in. This campaign's first problem is the second one.

## The material this campaign has to work with

[PDR-013](../decisions/PDR-013-explicit-core-red-line.md) already supplies the criterion — *does the core need it?* — and records why simplification kept stalling without one: every debate was argued case by case, protection had no boundary, and uniform protection turned out to be uniform friction. What the criterion has never been is systematically applied to a measured surface.

That criterion is not sufficient on its own, and [PDR-029](../decisions/PDR-029-simplification-tests-by-surface.md) records why. Applied to Cliewen's own methodology carriers it passes almost everything, because nearly every rule a skill states genuinely is needed by the core. So this campaign carries two tests, chosen by surface: *does the core need it?* for rules, artifact types, required fields, commands, and checks, and, for carrier prose, whether every rule-bearing statement traces to a live corpus artifact, states its rule once in the reading path, and admits checking. Connective statements are classified as carrying no rule and remain available for navigation and explanation. PDR-029 fixes the campaign's measure of success as the set of rules that survive those conditions — traceable, unduplicated, checkable, ordered by how binding they are, and consistent with one another — and explicitly not word count, because the second test sometimes makes a passage longer and a campaign scored on size would repair duplication by worsening compression.

**The shipped skills are this campaign's primary object.** They are what a reviewer evaluating Cliewen reads most closely, and they are read adversarially: the question asked of a skill is not whether it is short but, of a particular rule-bearing statement, *why do you have this statement?* The only answer that survives is a corpus artifact the reader can open. Cliewen already demands that discipline of a product — every behavior traceable to a criterion, every criterion to a capability, every capability to a goal — and its own instructions are the one surface where it was never applied. So the first two milestones are about the six skills and the routing hub alone: every statement classified, every rule-bearing statement traced, then the set trimmed and reordered against that register. M-064 carries the remaining carrier prose — the public guide, contributor guidance, and CLI text — so no carrier leaves this campaign unscored.

Order is part of the object, not a presentation detail. A carrier that states its inviolable rules after the procedure they constrain has arranged itself so the cheapest reading is the least safe one, and an agent that acts before finishing the file will have read the steps and not the prohibition. This repository's own skills currently do exactly that.

**The campaign asks rather than assumes.** A statement that traces to nothing may be waste, or it may be a real rule nobody ever wrote down; a statement that traces cleanly may still rest on a decision that has outlived its reason. Neither is settled by an agent. Both become open questions for the human — is this rule still needed, does it still make sense, is it important enough to bind every change, and why — and the answers become decisions. The campaign's target is a smaller set of rules that hold together, not a larger set that overlaps: two rules covering one situation is how ambiguity enters a methodology, and an ambiguous rule is obeyed inconsistently while appearing to be obeyed.

The two surfaces differ in how safely they can be cut, and that difference governs the whole campaign. Carrier prose is instruction: it can be rewritten, merged, or moved as long as every rule survives in the reading path. The corpus is the memory multiple humans and coding agents share, and its obligation is that a different reader, later, recovers the same meaning — so reducing what the corpus can remember is not simplification, whatever the read cost. M-064 is the one milestone that reaches that surface directly, and the part of it that does is scoped to make removal *legible* rather than to remove.

The measured cost is in [AN-006](../analysis/AN-006-plain-change-overhead.md) (overhead on changes that carry no meaning), [AN-010](../analysis/AN-010-adopter-change-overhead.md) (what a change costs an adopter rather than an authoring repository), and [AN-012](../analysis/AN-012-adopter-configuration-cost.md) (what adoption costs before any change happens). [AN-008](../analysis/AN-008-methodology-critiques.md)'s pattern C names the structural half: the graph only accumulates, and a corpus with no compaction path grows monotonically whatever its rules say.

[AN-013](../analysis/AN-013-distributed-work-and-evidence-boundaries.md)'s findings are a different matter and are recorded here so they are not lost. P-010's M-050 evidence names F1, F2's first half, and F3 as open findings feeding P-011, and P-011 closed without addressing any of them — but their histories differ, and this campaign owes each one a determination rather than a repetition of that sentence. P-009 declined F1's dependent-change candidate outright rather than deferring it, so what is open there is the decline's stated cost, not an unbuilt interface. [ADR-040](../decisions/ADR-040-qualified-external-references.md) consumed two of AN-013's candidates at once under P-009's M-044, so both remaining findings are narrower than they first read: it answered F3's qualified-reference candidate, leaving as open only whatever that decision did not reach, and it supplied the named-but-locally-unproven form F2 asked for, which leaves as F2's residue what AN-013 actually scopes to its first half — one repository's emitted CI wall kept in step by hand. That residue has never had a route at all. This campaign establishes for each whether it is answered, declined with a recorded cost, or genuinely open, and adopts or re-routes what remains; each of them adds machinery rather than removing it, which is exactly why the determination belongs here rather than in a milestone that assumes the answer. None of the three is dropped silently.

**F1 now has the human judgement M-065 was waiting on.** Answering M-062's escalations on 2026-08-08, the human asked that work depending on an unmerged change be recorded in the repository so the dependency is remembered until the merge happens. That is F1: the gap is that no artifact records the base, the dependency, or the authorization, so nothing local can distinguish proposed meaning from accepted meaning. P-009 declined the candidate rather than deferring it, and what a decline of that kind always lacked was a statement that the mechanism is wanted at its stated cost. It now has one, so M-065 designs and adopts the record for F1 rather than restating that it is open. F2's first half and F3 are untouched by this and still owe their own determinations.

## Milestones

Milestone numbering continues corpus-global numbering from [P-012](P-012-migration-gap-closes-on-evidence.md)'s M-061. M-067 was added after the campaign opened, by the declared plan revision riding with CH-130, and it sits in the table where it runs rather than where its number falls — M-066 closes the campaign and stays last, because a closing milestone that is not the final row invites exactly the mistake it exists to prevent. The order is load-bearing twice over. The skills come first because they are the campaign's primary object, not because they are the easiest surface. And within that, M-062 registers before M-063 trims, because every previous attempt at simplification argued case by case and stalled — a statement cannot be defended or removed on merit until someone has written down what it is for.

| ID | Milestone (exit criterion) | Status | Evidence |
|---|---|---|---|
| M-062 | **Every statement in the shipped skills and the routing hub is classified, every rule-bearing statement is traced, and the unanswerable rules reach a human**: a spike walks the six generated skills and `AGENTS.md` statement by statement, classifies each as rule-bearing or connective, and records a rule-bearing statement's live corpus trace — a decision, constraint, goal, or criterion — or the fact that it traces to nothing; whether a live carrier already states that rule in the same reading path; and whether a reader can determine its satisfaction. A heading, navigational sentence, worked example, or other connective statement carries no obligation and is recorded as carrying no rule rather than forced through those three conditions. A goal or general constraint is a valid trace when the statement directly restates an obligation that artifact states, but derivation from broad intent is not a trace. The register also assesses order, reporting every statement that binds absolutely but is read after the procedure it constrains. Duplication is counted per reading path rather than per file, so [ADR-021](../decisions/ADR-021-generated-standalone-skills.md)'s deliberate file-level repetition is not scored as a defect. Three classes are escalated rather than decided: a rule-bearing statement that traces to nothing, a statement whose decision may itself have outlived its reason, and a pair of obligations over one situation that could pull a reader in different directions. Each becomes an open question naming what it traces to or fails to, what removing or retaining it would cost, and what human judgment is required — answered by a human and recorded under [C-011](../constraints/C-011-decision-records-typed.md). The register is written to be read by someone who did not build it. The milestone also recommends what durable form the register should take; the human's answer to that recommendation is what settles it. **The carriers themselves stay clean: no trace, citation, or marker is added to a skill or to `AGENTS.md`.** The register is a separate artifact in this repository, and the design problem that creates is drift — a map kept beside the prose it describes, with no mechanism, is the second stored representation [PDR-028](../decisions/PDR-028-derived-report-is-not-a-committed-registry.md) refused, and it goes stale on the first skill edit with nothing reporting it. So the milestone fixes what counts as a single statement, precisely enough that an independent second pass which did not write the definition segments the same prose the same way. It reports the population each class yields, and recommends whether and how a register row can stay bound to a statement it does not annotate — including whether an edited statement should invalidate its row and force the rule to be restated, and what a recommendation to leave the register unbound would cost. A recommendation that `clue` enforce the register changes what a green `clue validate` asserts and is core-adjacent under [C-013](../constraints/C-013-core-changes-need-decision.md); a recommendation that it stay a pinned point-in-time artifact states what that costs a reviewer who reads it a year later. Building it is a plan revision adding a milestone, backed by its own decision record and human acceptance — this milestone recommends and does not build. The register also records compatible overlap candidates that M-063 can consolidate. Nothing is removed by this milestone. | `done` | CH-130: [AN-018](../analysis/AN-018-skill-statement-register.md) registers every statement in `AGENTS.md`, the six generated skills, and the six shared fragments against a stated segmentation rule — classified, traced to the narrowest live artifact or to nothing, scored for in-path duplication and checkability, and assessed for order — with its populations, eight compatible overlap candidates for M-063, and a recommendation that the register stay a pinned artifact rather than gain a mechanism. Eight escalations; Q-01…Q-07 answered 2026-08-08 and recorded as PDR-030, PDR-031, PDR-032, an amendment to PDR-006, and five `log.md` rows; Q-08 remains open. No carrier was edited. |
| M-063 | **The shipped skills and routing hub carry only traceable rules, ordered by how binding they are**: the set is trimmed and reordered against M-062's register and its answered questions, so that every surviving rule-bearing statement passes all three of [PDR-029](../decisions/PDR-029-simplification-tests-by-surface.md)'s carrier conditions, connective statements remain only where they aid navigation or understanding, and each carrier states its inviolable rules before the procedure they constrain. A duplicated rule is stated once; compatible overlapping obligations are consolidated; and a genuine conflict is resolved only as its human-approved decision directs, including precedence, reconciliation, or retirement. Meaning is preserved even where wording is not: every rule a removed statement carried is still stated somewhere the reading path reaches unless a human-approved decision explicitly supersedes or retires that obligation, **and** every decision record whose own `Carrier` line points at removed prose is updated in the same change — the inventory obligation runs in both directions ([PDR-019](../decisions/PDR-019-methodology-contract-carriers-move-together.md)). Editing methodology carriers makes this a full-tier change. [C-013](../constraints/C-013-core-changes-need-decision.md) applies only to an edit that changes what the verifiable thread, human merge boundary, or deterministic judge means; touching `AGENTS.md` or a skill does not trigger it by itself. The change edits the generator source under `internal/skills/source/` rather than generated output ([ADR-021](../decisions/ADR-021-generated-standalone-skills.md)) and updates the scaffolded adopter copies. Section order lives in the skill templates rather than the generator, so reordering needs no generator change; it does require replacing the positional cross-references the current prose relies on — a shared fragment cannot say "below" once its position differs between the skills that render it — and it moves the generator's pinned expected-output strings, which is a test tracking output that legitimately changed rather than a check weakened to pass ([C-004](../constraints/C-004-never-weaken-checks.md)). Evidence re-runs the register showing no untraceable, duplicated-in-path, uncheckable, unresolved-conflict, or out-of-order rule-bearing statement remaining, and states the surviving rule set rather than a word count. | `todo` | |
| M-064 | **The rest of the surface is scored and settled**: every remaining rule, artifact type, required field, command, and check is scored against *does the core need it?*, and the carrier prose M-062 and M-063 did not reach — the public guide, contributor guidance, and CLI text — is classified statement by statement, with every rule-bearing statement scored against PDR-029's three carrier conditions and every carrier scored against its ordering rule. Each candidate is either carried out, with a guard proving that what the core needs still resolves, or declined with its cost stated, in the manner [PDR-026](../decisions/PDR-026-campaigns-close-on-re-derived-evidence.md) requires of a declined request. This milestone also determines [AN-008](../analysis/AN-008-methodology-critiques.md)'s pattern C explicitly — closed, declined with a stated cost, or routed to the successor campaign — and closing it means a retired or superseded artifact provably leaves the mandatory read path as `clue context` and `clue validate` compute it, a decision carries a supersession edge so that reversing one answers what was downstream of it, and the born-`inferred` population is bounded rather than monotonic. Because that reaches the shared-memory surface, nothing removed from the read path may become unrecoverable: every retired artifact's meaning stays reachable by a route this milestone's evidence names. A candidate that changes what the verifiable thread, the human merge boundary, or the judge means carries its own decision record under [C-013](../constraints/C-013-core-changes-need-decision.md); merely touching a carrier is insufficient. No candidate and no finding is silently dropped. | `todo` | |
| M-065 | **[AN-013](../analysis/AN-013-distributed-work-and-evidence-boundaries.md)'s three open findings each carry a determination**: F1, F2's first half, and F3 are each answered with a named mechanism, declined with a recorded cost, or routed to a successor campaign as an explicit milestone. Each adds machinery rather than removing it, so each determination states why the addition is warranted inside a simplification campaign or why it is not. None is dropped silently, and the repetition of P-010's and P-011's "still open" sentence does not satisfy this milestone. | `todo` | |
| M-067 | **The answers M-062 produced reach their carriers and their machines**: every mechanism the human's answers to M-062 asked for is built, each with the evidence its class requires. A constraint's `source:` must resolve to a live file rather than merely be non-empty, the no-computed-figures rule is minted as a constraint with `enforcement: agent` and a stated promotion trigger under [ADR-017](../decisions/ADR-017-conventions-are-constraints.md), and `clue-verify`'s unbounded "assessed against every constraint" item is repaired — the register found that defect independently, and the answer to whether constraints are actually applied lands on it. The rule that a reference names what it is about is minted as a constraint with `enforcement: human` and a priced residual, and stated in the carrier that reaches agent-facing reports; no machine will hold it, because `clue validate` reads files and cannot see a report. [PDR-032](../decisions/PDR-032-mid-change-suggestions-are-triaged.md)'s triage step reaches the change loop's carriers and the workspace template. The hub gains its missing `clue-extract` row, guarded in two layers: a generator test asserts every name in `skillNames` appears in this repository's hub and in the scaffolded template, catching the omission when a skill is added, while an adopter's missing rows are a counted population on `clue validate`'s OK line and never an issue, following [ADR-046](../decisions/ADR-046-index-rows-say-what-the-artifact-is-about.md)'s precedent — the hub is the adopter's own file under [ADR-013](../decisions/ADR-013-ships-generic-vs-repo-local.md). F-RB-09's orient-after-merge rule moves from the review-boundary fragment to `durable-work`, whose subject it actually shares, and the shared `decision-records` fragment gains [PDR-006](../decisions/PDR-006-decision-records-are-typed.md)'s rejected-record clause, which that record's carrier line names as owed here. Editing methodology carriers makes this full tier; the change edits `internal/skills/source/` rather than generated output and updates the scaffolded adopter copies. | `todo` | |
| M-066 | **The campaign closes on re-derived evidence**: the cost surfaces [AN-006](../analysis/AN-006-plain-change-overhead.md), [AN-010](../analysis/AN-010-adopter-change-overhead.md), and [AN-012](../analysis/AN-012-adopter-configuration-cost.md) measured are measured again at head, and the result states what changed, what did not, and what the campaign declined — re-derived from the corpus and the tool rather than read off this table, as PDR-026 requires. This milestone's digest closes the campaign and designates a successor when one is decided. | `todo` | |

## Mutation rules

Once this campaign opens and its milestone table exists, status and evidence fields in that table may mutate in an implementing change's merge digest. Everything else changes only through a declared plan revision backed by a decision record routed by reversal cost. Plan adjustments are decisions.
