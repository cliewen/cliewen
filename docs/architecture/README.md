# Architecture

System scope: the whole, the cross-cutting, the expensive-to-change. Per-capability *design* is colocated with its capability — one home per scope, or the trees drift apart. All diagrams are Mermaid in fenced blocks, inline; a diagram too large to read is a design problem, not a rendering problem.

<!-- clue:index:start -->
- [architecture.md](architecture.md) — actors, lifetime classes, the frontmatter graph
- [skills.md](skills.md) — the skills layer: why the set looks like it does, hand-offs, rules for future skills
<!-- clue:index:end -->
