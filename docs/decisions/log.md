---
id: LOG-001
type: log
status: active
links: [PDR-003]
title: Decision log
---

# Decision log

Decisions that are cheap and local to reverse, one row each — newest first. **The cost of undoing a decision is the sole routing test** ([PDR-006](PDR-006-decision-records-are-typed.md)): cheap and local to reverse → a row here; expensive to reverse → a full record — ADR for architecture, PDR for project/process. A rule's reach does not change its routing: a decision can apply to every future change and still be a log row, as long as undoing it stays cheap and local (this table's coverage-gate row is the precedent). Demotion/promotion mechanics are [PDR-003](PDR-003-decision-log.md). Rows are never deleted; a reversed decision gets a new row, and a row whose reversal turns out to be expensive after all is promoted to a full record citing this table. Full text of demoted records lives in git history — and, from [ADR-034](ADR-034-retirement-is-deletion.md) onward, this file's own `supersedes:` frontmatter names the ID of every ADR demoted into it, so `clue validate` can confirm the demoted file is actually gone.

| Date | Decision | Why | Change/PR |
|---|---|---|---|
| 2026-08-08 | `clue-delta`'s CH-number instruction is repaired in its own change rather than folded into the simplification trim: the skill still tells an agent to take the next free number by searching Git history, which [ADR-048](ADR-048-corpus-wide-id-ledger.md) superseded for every native prefix and whose ledger, `clue id next`, `clue id live`, and `checkLedger` rule are all built and live. That repair replaces the instruction with the command and corrects ADR-048's own closing paragraph, which still describes the mechanism as unbuilt follow-up work. `clue-extract`'s brownfield minting rules are deliberately out of its scope | The defect is not a wording drift but an instruction that routes an agent into a failing `clue validate`, measured rather than argued: CH-130 followed the skill as written, derived its number by grepping history, and was rejected twice before the ledger was consulted. Waiting for M-063 leaves a live carrier lying for the length of a trim that is itself blocked on other answers, and folding it into CH-130 would breach M-062's own boundary that the register removes and rewords nothing. Routing is what is decided here, and rerouting it costs nothing; the repair itself is a full-tier carrier change under [PDR-019](PDR-019-methodology-contract-carriers-move-together.md) because it moves the skill, both scaffolded copies, and an ADR paragraph together | CH-130 |
| 2026-08-08 | The rule that a durable record never states a figure a command computes becomes a constraint artifact rather than staying unrecorded prose, and `checkConstraints` is tightened so a constraint's `source:` must resolve to a live file instead of merely being non-empty. Both land with `clue-verify`'s repair of its own unbounded "assessed against every constraint" checklist item; the constraint is minted in the change that writes its carrier prose, not before, because `source:` names the document that states the rule | [ADR-017](ADR-017-conventions-are-constraints.md) already says every prose-only convention becomes a constraint artifact, and nothing has ever checked that it does — so the register is a promise the corpus keeps by habit. The general check cannot be written: [C-006](../constraints/C-006-adrs-timeless-with-carrier.md)'s residual states that nothing derives the set of live carriers for a given contract, and deciding that a paragraph *should* have been a constraint is a judgement about English. What can be held mechanically is narrower and worth holding — a register entry whose source points nowhere is unfalsifiable, and the checklist item asking for assessment against an unbounded set is a box no one can tick honestly. Reversal is one slice in `rules.go` and one constraint file, so it is cheap and local; the constraint's own enforcement class and promotion trigger carry what stays with judgement | CH-130 |
| 2026-08-08 | A reference names what it is about: a report, status, or explanation written for a human or an adopter never leaves a bare `ADR-018` or `CH-008` standing alone, but says in a few words what the record holds, and a status is short and precise rather than a chain of identifiers. This registers as a constraint with `enforcement: human` and a priced residual, minted in the change that writes its carrier prose | This is [ADR-046](ADR-046-index-rows-say-what-the-artifact-is-about.md)'s rule applied to the surface it was never applied to. An index row that states only its filename and a status line that states only an ID fail the same way: the reader learns that something exists and nothing about whether it matters to them, and the cost lands hardest on the adopter, who has none of the context that makes an ID legible. `clue validate` reads files and cannot see a report, so no machine will ever hold this; saying so in the residual is more honest than a promotion trigger that will never fire. Reversal is one constraint file and the carrier sentence it points at | CH-130 |
| 2026-08-08 | The review boundary's authorization escape does not reach plain changes: work that must build on an unmerged change becomes a blocking open question the human may explicitly authorize, and a plain change is the one class where that authorization is unavailable, as the routing hub already states absolutely | [PDR-011](PDR-011-plain-changes-bypass-cliewen.md) already states this verbatim — "Plain PRs do not consume the one-Cliewen-change-in-flight slot. They may proceed independently but never build on an unmerged branch" — so nothing new is decided here and the routing stays cheap: what is recorded is that the shared review-boundary fragment drifted from a verified decision by stating the authorization escape generally and exempting nothing, and that aligning the fragment is the repair. The register that found this had mis-traced the hub's rule and read the pair as an unadjudicated conflict; it is not one, and saying so keeps a settled decision from being re-argued as though it were open. The reason PDR-011 gives is mechanical rather than a preference: a plain change carries no CH identity, no proposal, and no workspace, so there is nowhere to record an authorization, and an authorization that leaves no artifact is what the escape exists to avoid. Reversal is one clause in one fragment | CH-130 |
| 2026-08-08 | This repository's routing hub gains the `clue-extract` row its skills table is missing, and forgetting such a row becomes checkable in two layers: a generator test asserts that every name in `skillNames` appears in both this hub and the scaffolded template, while `clue validate` reports an adopter hub's missing skill rows as a counted population on its OK line and never as an issue | [ADR-043](ADR-043-upgrade-skill-is-a-managed-carrier.md) states a managed set of six and this hub lists five, so the reference implementation and the template it ships disagree about what the skill set is. The two layers are split by who owns the file: adding a skill is something this repository does, and a generator test catches the omission at the moment it happens, which is where the forgetting actually occurs; an adopter's hub is their own file under [ADR-013](ADR-013-ships-generic-vs-repo-local.md) and some will omit `clue-extract` deliberately, so failing their corpus over a row they chose not to write would be a new obligation bought for nothing. Counting it follows the precedent [ADR-046](ADR-046-index-rows-say-what-the-artifact-is-about.md) set for index rows that say nothing. Reversal is one row, one test, and one counter | CH-130 |
| 2026-08-04 | A milestone status cell in a plan table is one of `todo`, `doing`, `done`, or `dropped`, and `clue validate` checks it. The column is found by its `Status` header, so a table that declares no status column is not one this rule reads, and a `dropped` milestone states its reason in the evidence column the same way a `[-]` task states it on its line | [C-010](../constraints/C-010-milestone-status-vocabulary.md) could not be machine-enforced without a vocabulary to check against, and none had ever been declared — the constraint said so in its own promotion trigger. This codifies what ten campaigns already did: every milestone row in P-001…P-010 reads `todo` or `done` and no plan has ever used a third value. `doing` and `dropped` are added because the two states exist and were being expressed in prose; naming them keeps a real state from being recorded as its nearest legal neighbour. Reversal is one slice in `rules.go` plus any row that used a withdrawn value, and no plan's meaning moves, so it is cheap and local — the lasting contract is that the judge reads plan tables at all, which [ADR-044](ADR-044-judge-reads-state-not-transitions.md) already settles by making this a property of the corpus as it stands rather than of a change | CH-110 |
| 2026-08-04 | The release cache stores a non-answer as well as an answer: when the release list does not answer — offline, a timeout, a rate limit, or a reply the command does not recognize, which ADR-042 already treats as one outcome — that is remembered for an hour under its own field, and only a check nobody asked for honours it — `clue latest` always asks. [ADR-042](ADR-042-release-check-outside-the-judge.md) is untouched and needs no successor: its rule that an unreadable, corrupt, or unwritable cache is simply ignored governs the cache *mechanism* failing, which is a different failure from the release list failing, and the two are kept apart in the file itself — a remembered non-answer is an explicit field, so a truncated or self-contradictory cache stays absence and can never silence a request | The day-long cache's promise, that repeating the check costs no request, held only when the list answered: nothing was stored on failure, so every workflow command in an offline or blackholed session paid the ambient budget again for the same non-answer, which is precisely the cost the notice may never have. An hour is the lifetime because a non-answer is a fact about a moment rather than about the world — it covers one sitting of work and GitHub's unauthenticated rate-limit window, and it does not punish someone for the rest of the day for having fixed their network. The asymmetry is what makes remembering safe at all: a requested check always asks, so this can bound what an unrequested one costs without ever hiding the answer from someone who wants it. Reversal is one field and one branch in one package, with no corpus format, no adopter migration, and no promise withdrawn, so it is cheap and local — but it changes what the cache file means, so it is carried by [AC-088](../capabilities/CAP-004-ship/criteria.md) rather than slipped in under AC-086 | CH-109 |
| 2026-08-02 | All 27 remaining `inferred` decisions — ADR-023, ADR-025…ADR-040, and PDR-013…PDR-022 — are promoted to `verified`, signed `Flemming N. Larsen (2026-08-02, conversation)`. The sequence is recorded exactly, because this record's own subject is what a signature may claim: the human stated the approval for all of them first; the complete list was then put in front of them by identity and title, together with two consequences — that ADR-029 governs this very operation and would be promoted by it, and that ADR-039 and PDR-022 constrain P-010's own remaining milestones — and the approval was carried forward with no record held back and no objection raised. It is one blanket approval made informed, not twenty-seven individual readings. `author: agent` is unchanged on every record, and no record's text was edited. The bar for future decisions is unmoved: new agent-authored decisions are still born `inferred` with `accepted-by: []`, and merging still does not sign them | Merging makes an agent-authored decision binding without verifying it, which is exactly what [PDR-004](PDR-004-merge-binds-approval-signs.md) and [ADR-029](ADR-029-accepted-by-is-cliewen-approval-only.md) intend — so the backlog was correct behaviour, not a defect. What it had become was a corpus where nearly half the decisions binding this project had been written by an agent and never read back by the human they bind, including the core's red line and the rule defining what a signature may record. Promotion is two frontmatter lines per record and an objection reverts one file and opens a question, so the act is cheap and local to reverse; what is not cheap is a signature that was not meant, which is why the list was presented in full before the approval rather than summarized after it | CH-105 |
| 2026-08-02 | P-010 opens `active` as P-009's successor, with sequential milestones M-045…M-050 in two arcs: first make a repository able to tell it is behind and show it the catch-up recipe for the machine it is on, carry that routing to already-onboarded adopters through a skill, and surface it when a coding agent starts; then clear the three backlogs the judge already counts — 27 `inferred` decisions, 13 agent-enforced constraints, and four capabilities contradicted by AN-007, AN-011, and AN-013. Simplification is deferred to P-011 rather than folded in | P-009 built the mechanical upgrade path and stopped there: `clue migrate` is named in an adopted repository only as a precondition of a change already under way, never as an entry point an agent could be routed to; "upgrade" appears in none of the five skills; and the emitted routing hub is the adopter's file, so routing added there reaches new repositories only. Worse than missing guidance is confident silence — an old binary with matching old skills validates green and reports "no changes needed", which reads as being current. The backlogs come first in scope and second in order because the visibility work is what an adopter meets this month, while the backlogs are Cliewen's own meaning and form: a method promising that humans verify meaning and machines enforce form cannot leave nearly half its decisions unverified and all but two of its constraints unenforced by any machine. Opening, reordering, or closing an unstarted campaign is a status flip and cheap to reverse; each milestone routes its lasting contract through its own ADR or PDR | CH-104 |
| 2026-08-02 | Closing a plan is stated as digest bookkeeping in every live carrier and checked by the pre-merge checklist: the change completing the last milestone sets the plan `completed` in that digest, designating a successor there when one is decided | The rule was already the practice (P-007, P-008) and already in this log, but no carrier said whether "plan bookkeeping" included the plan's own status. Seven said it generically, so the ambiguity was uniform and survived: CH-095 ticked M-044, left P-009 `active`, and argued in its commit message for a distinction P-008 had already rejected by closing itself and naming its successor in one digest; CH-099 existed only to repair that. One of the seven was `clue-verify`, whose checklist CH-095 passed unchanged — ticking a milestone does reflect the merge, and no completed plan changed — so the rule is now stated where an agent reads and asked where an agent checks. A rule stated nowhere an agent reads is not a rule, and one no checklist asks for is not verified. Nothing is decided here that was not already practice, and reverting one clause per carrier restores the previous text exactly | CH-102 |
| 2026-08-01 | P-009 is closed `completed`. Its successor is not designated here | All seven milestones (M-037…M-042, M-044) are `done` with evidence and nothing waits on the campaign, so leaving it `active` misstates what is in flight in the index that doubles as the achievement overview — the same-digest closure follows P-007's and P-008's precedent. The successor is deliberately left to its own change: what Cliewen does next is a scheduling decision, and bundling it with a closure would settle it as a side effect | CH-099 |
| 2026-08-01 | P-009's M-044 is widened from the foreign-evidence form alone to the whole outward-reference boundary, absorbing AN-013's candidate 3: qualified external references, the offline form rule, the separated resolver, and the report-only adopter migration land with the evidence pointer rather than after it | A pointer to foreign evidence names a repository, a revision, and an identifier, but while a bare reference cannot say which repository it means, that pointer has nothing to stand on — AN-013 says so directly, and its candidate 3 was left as a door that any corpus-touching change could ride. Building the two separately would ship the dependent half first. Widening an unstarted milestone is cheap and local to reverse, and [ADR-040](ADR-040-qualified-external-references.md) carries the lasting contract | CH-095 |
| 2026-08-01 | P-009's M-043 is removed: Cliewen does not record a dependent change's base or its human authorization as durable data, so a reader of history sees the merge but not what it was built on or who permitted it. The review boundary's existing rule is unchanged — an agent building on unaccepted work records a blocking open question and stops unless the human explicitly authorizes it — and [AN-013](../analysis/AN-013-distributed-work-and-evidence-boundaries.md) remains the record of the gap. M-044 stays and is implemented next; P-009 closes on it | The supported path already discourages the situation the interface would describe: one Cliewen change is in flight per author, every change branches from accepted `main`, and stacking needs explicit human authorization first. Building durable authorization data for a path the methodology tells you not to take spends the campaign's remaining budget on the wrong gap, while the foreign-evidence form in M-044 is one an adopter meets in ordinary work. Removing an unstarted milestone is cheap and local to reverse — re-adding it restores the candidate untouched, as CH-086 already did once in the opposite direction — and the declined boundary is stated here rather than left as silence | CH-094 |
| 2026-08-01 | CH-093's report-only rehearsal lands durably as [AN-015](../analysis/AN-015-tank-royale-migration-rehearsal.md), a one-off explicitly directed by the human for this rehearsal only; PDR-020's contract that a rehearsal changes no Cliewen corpus and that the mutate phase owns the durable extraction report stands unchanged, and no general permission is created | A narrower support boundary recorded solely in a deleted workspace is not recorded: the four human answers, the one open M-042 conjunct, and the Tank Royale doors would have left no trace on `main` beyond one plan evidence cell. Deleting the one landed file undoes the decision entirely, which is what keeps it a log row rather than a PDR-020 amendment | CH-093 |
| 2026-07-31 | When releasing this repository, refresh the locally installed `clue` from the checkout with `go install ./cmd/clue` before release verification; ordinary changes and adopter repositories do not trigger that local installation update | A stale PATH binary can judge an older source and skill set during Cliewen's own release work, while mutating a user's installation as a side effect of unrelated work would exceed this repository-local convention | CH-088 |
| 2026-07-30 | After its migration-readiness phase, P-009 continues with sequential milestones M-043…M-044: first make a dependent change's base and human authorization durable, then give foreign acceptance evidence a named but locally unproven form | AN-013 reproduces both gaps and names P-009 as their consumer. Scheduling them after M-042 keeps them outside the prerequisites for a truthful repository migration while preserving their rejection boundaries: the human merge boundary remains controlling, forge state carries no meaning, a foreign green check is not imported as proof, and the deterministic judge does no network resolution. The process contract comes first because the foreign-evidence form changes corpus and validator contracts; removing or reordering unstarted milestones is cheap and local to reverse, while each milestone routes its lasting contract through its own PDR or ADR | CH-086 |
| 2026-07-30 | P-009 opens `active` with sequential milestones M-037…M-042: eliminate JVM false-green attribution, preserve stable brownfield criterion IDs, make merge-history support explicit, replace the forked CI wall, add mechanical corpus upgrades, and finish with an anonymous report-only migration rehearsal | AN-012 already routes the non-forking wall and mechanical upgrade path here, AN-013 shows ADR-009's promised per-test JVM carrier was never installed, and AN-014 records the anonymous assessment's limits while reproducing the current harvester, ID grammar, and merge-mode boundary locally. AN-014 keeps reported enterprise CI policies as hypotheses until M-040 proves them with reviewable fixtures. Ordering correctness and identity before operational convenience keeps a migration from becoming green by losing evidence or provenance; closing or reordering the unstarted campaign is cheap and local to reverse, while each milestone routes its lasting contract through its own ADR or PDR | CH-085 |
| 2026-07-30 | P-008 is closed `completed`: all five milestones (M-032…M-036) are `done` with evidence, and **P-009 is designated its successor**, consuming AN-012's and AN-013's routed candidates | The campaign's exit criteria are satisfied and evidenced by CH-079…CH-083; M-036 was its last milestone, and both closing analyses name a successor plan as the consumer that cannot be proposed while P-008 stays open. Leaving a plan `active` with nothing left to do misstates what is in flight in the index that doubles as the achievement overview; the same-digest closure follows P-007's precedent, and a status flip is cheap and local to reverse | CH-083 |
| 2026-07-30 | Cliewen implements no distributed-work interface for stacked changes, cross-repository evidence, or tracker metadata. Two boundaries bind every future candidate: nothing may weaken the human merge boundary, and nothing may make forge state the system-of-record — so an imported foreign green check, an automated stacked merge, a base branch read as accepted meaning, and cross-repository link resolution inside `clue validate` are all rejected outright. The measured needs are routed as P-009 candidates: a dependent change that declares its base and authorization as durable data, a named-but-locally-unproven form for foreign acceptance evidence, qualified external references with a lint rule behind them, and a decision on ADR-009's undelivered JVM carrier | AN-013 reproduced all three boundaries on a pinned repository pair. A dependent change validates green with nothing distinguishing its base's unaccepted meaning from accepted truth, its merge alone binds that base, and a base reversed in review is absorbed by a clean merge — because no artifact records the base, the dependency, or the authorization. Cross-repository evidence already exists in both directions, is discharged by hand, and cannot be written down at all: any foreign identifier in `links:` fails, leaving `Test-type: Human` and `@draft` as the only honest expressions. Forty-eight unqualified forge references span four repository namespaces and one already resolves to an unrelated live pull request in Cliewen's own. Declining to build the interfaces is cheap and local to reverse; each of the convenient designs would be expensive and would cost a core promise | CH-083 |
| 2026-07-29 | Cliewen implements no adopter configuration interface: ADR-013's rejection of a machine configuration file stands, and its condition — that `clue` itself need repo-local settings — remains unmet. The measured adopter needs are routed instead as P-009 candidates: a CI wall that need not be forked to be adapted, and a mechanical corpus-upgrade path | AN-012 priced all three assumption families P-007 deferred behind a pilot on the pinned first adopter. Agent-directory placement and local-skills coexistence cost it zero edits and zero failures — AC-038's symlink handling and ADR-022's ownership marker already covered both — and the CI wall's runner, action, and install-path assumptions matched the repository's own conventions exactly. Its one wall edit changed how the binary arrives; the maintainer confirms this was a preference, not a mandatory adopter need, and configuration expresses it no better than an edit does. A version bump it has not attempted starts from 69 blocking corpus issues. Declining to build an interface is cheap and local to reverse; a second source of truth for agent routing is not | CH-082 |
| 2026-07-29 | `clue-analysis` classifies verification results as clean disposable or prepared environments; any local prerequisite makes a result prepared, whose prerequisites it names and which cannot claim onboarding reproducibility. Statistical or percentage evidence names its versioned corpus and population, eligibility rules, exclusions with reasons, sampling or repetition method, uncertainty, and deterministic-versus-quality boundary; adoption analysis names the governance or process changes it introduces rather than describing scaffolding as neutral | AN-003 found that a passing prepared build was not clean-clone evidence, that an informal population can bias a percentage claim, and that adoption changes governance; AN-004 and AN-005 independently confirmed that conditions and environment-sensitive quality materially change what evidence demonstrates. The guidance remains a local skill rule and is cheap to revise | CH-080 |
| 2026-07-28 | P-008 opens `active` with sequential milestones M-032…M-036: repair the complete live evidence-carrier inventory first — core, corpus, architecture, and implementation explanations, shipped CLI/plugin/site entry points, public and contributor guidance, capability status and design, repository and scaffold workflow templates, and canonical plus generated lifecycle skills — harden analysis and extraction from reproduced findings next, with M-034's report-only rehearsal held in its change workspace until authorized mutation digests it into `/docs/analysis`, and require adopter evidence before choosing configuration or distributed-work interfaces | AN-011 shows a present contradiction across those live carriers, while AN-003 and AN-008 distinguish demonstrated workflow gaps from interfaces that still lack enough evidence to design; M-032 stays one wide change because splitting the carrier repair would put a self-contradictory state on `main` and break the rule that milestone establishes; the change workspace gives M-034 a reviewable handoff without mutating the target corpus or contradicting the extraction report contract; closing or reordering an unstarted campaign is cheap and local to reverse, while each implementing milestone routes its own lasting decisions | CH-078 |
| 2026-07-27 | `.goreleaser.yaml` carries no `changelog:` block; passing `--release-notes` is the whole mechanism that keeps the generated commit dump out of the release body | `changelog: {disable: true}` reads as belt-and-braces for [ADR-012](ADR-012-release-notes-from-changelog.md) and is documented to discard `--release-notes` too — it published v0.8.0 with a blank page. Deleting a config key is cheap to reverse; the durable guard is the workflow's post-publish comparison, not the key | CH-069 |
| 2026-07-26 | The tag-versus-skill-stamp invariant is enforced by the release workflow running the shipped judge stamped as the tag (`clue validate --forbid-changes` built with `-X main.version=<tag>`), not by a new corpus rule and not by a shell comparison of skill frontmatter | The rule already exists and already ships ([ADR-011](ADR-011-version-stamping.md), `checkSkillVersions`); only its invocation was missing, and the tag is visible only to the workflow, so no corpus rule could see both halves. Running the shipped rule keeps one definition of the invariant instead of a shell copy that can drift from it, and it bites where the mismatch can still be prevented — the same placement argument [ADR-012](ADR-012-release-notes-from-changelog.md) makes for the release-notes gate. Reversing it is deleting one workflow step | CH-065 |
| 2026-07-25 | P-007 (core hardening) opened `active` with milestones M-024…M-029, consuming AN-008's digest of the two 2026-07-25 critiques | Both reviews land on half-built faces of the core elements; leaving them unscheduled wastes the sharpest evidence since the first adoption, and closing an unstarted plan is a cheap status flip | CH-063 |
| 2026-07-25 | P-006 is closed `completed`: all four milestones (M-020, M-021, M-022, M-023) are `done` with evidence, and no successor plan is designated | The campaign's exit criteria are satisfied and evidenced by CH-057, CH-059, CH-060, and CH-061; leaving the plan `active` after its work is finished would misstate what's in flight | CH-062 |
| 2026-07-25 | A MADR record whose status is `proposed` converts to a decision record only where the repository has in fact been living by the proposal, with the body saying so; a proposal never acted on becomes an open question in the extraction report and a named plan door instead, and the report states which way each `proposed` record went. A `rejected` record converts to a kept record whose body states the rejection outright, in `## Decision outcome` or a `### Rejected: <option>` section | Merging a change makes its `inferred` decisions binding, so converting an untouched proposal would manufacture a binding decision no one ever made — the mirror image of the acceptance inflation [ADR-029](ADR-029-accepted-by-is-cliewen-approval-only.md) forbids, and equally not reversible once the source is deleted. Both halves keep every source record's meaning and differ only in which carrier holds it, which is what leaves the choice local to one source format | CH-061 |
| 2026-07-25 | When two MADR source records collide on the same number (the usual leftover of a `doc/adr/` → `docs/decisions/` migration), the record whose source path sorts first keeps the number and the rest are minted fresh IDs; minting runs only after every preserved number is placed, so a minted ID can never collide with one kept verbatim | A tiebreak that is left unstated is not a mapping at all — two agents converting the same corpus would produce different IDs, which is exactly the improvisation the mapping exists to remove. Path order was chosen because it needs nothing but the file names, so it is reproducible without reading dates or git history; any other total order would do equally well, which is what makes the choice cheap and local to reverse | CH-061 |
| 2026-07-25 | A MADR record whose status is `deprecated` or `superseded by …` converts, like every MADR record, to a kept decision born `inferred`; the source status survives as meaning rather than being dropped — a body-prose sentence states what the source status was, and where a superseding record exists the converted record also carries a `links:` edge to it. `rejected` is not re-decided here: [docs/decisions/README.md](README.md) already holds that rejected alternatives stay in the corpus and decisions are never deleted, so a `rejected` record converts to a kept record under that existing rule | An extraction deletes its MADR source in the same PR, so a collapse rule that drops meaning is not reversible where it matters — the rule has to be lossless on the first pass. Routing the choice of carrier (body prose, a `links:` edge, or both) to a log row keeps it cheap and local to reverse for a later mapping revision, without ever risking the meaning itself | CH-061 |
| 2026-07-25 | `clue-extract`'s target contract names assistant entry points as a class (`AGENTS.md` flagship, plus `CLAUDE.md`, `.cursor/rules`, and similar) rather than assuming `AGENTS.md` alone; states that leaving an extracted capability `status: draft` for size — not just untestability — is the sanctioned way to phase a corpus too large to tag-test in one change; and adds deterministic sequential ID minting, per capability's `ac-prefix:`, for source requirements with no stable ID of their own | Tank Royale's extraction found all three gaps as real friction: a repository with more than one assistant file, a corpus too large to fully test in one PR, and requirements with no existing IDs. Stating the resolution in the contract is a phrasing clarification of an agent-facing skill, cheap and local to reverse — it changes no `clue` behavior and adds no new machine-checked rule | CH-060 |
| 2026-07-25 | `clue init` never writes through a symlinked directory below the target root: any symlinked ancestor of an emitted file blocks that subtree, which is reported as its own `linked` category rather than followed, and no flag overrides it. The root itself is never inspected, and `clue validate`'s deliberate link-following ([ADR-028](ADR-028-deterministic-skill-manifest.md)) is untouched | A folder shared across checkouts belongs to more than the repository being initialized, so writing into it is outside `init`'s mandate — and doing so silently is worse than not doing it at all. Skip-versus-follow is a real direction choice, but `init` emission is periphery rather than the deterministic judge, and reversing it is a local change to one helper | CH-059 |
| 2026-07-24 | P-006 opens as the campaign that digests Tank Royale's adoption findings: four milestones (M-020 filesystem-independent skill discovery, M-021 `init` against a real skills folder, M-022 the extraction contract's entry points, phasing, and ID minting, M-023 a MADR mapping), with the two findings that arrived already closed recorded as history rather than scheduled | The first repository to live with Cliewen recorded exactly where it cost time, and that evidence is unique — every finding left undigested is rediscovered by the next adopter at the same price. Scope and ordering are cheap and local to reverse; the milestones' individual decisions are not, and each implementing change routes its own | CH-056 |
| 2026-07-24 | P-005 is closed `completed`: all four milestones (M-016, M-017, M-018, M-019) are `done` with evidence, and no successor plan is designated | The campaign's exit criteria are satisfied and evidenced by CH-051, CH-052, CH-053, and CH-054; leaving the plan `active` after its work is finished would misstate what's in flight | CH-055 |
| 2026-07-24 | The canonical change-tier text (`internal/skills/source/shared/change-tiers.md.tmpl`) states routing as three named rules — plain, light, full — plus two escalation guards — an unclear tier takes the higher one, and a decision, open question, meaning change, or methodology-carrier edit discovered mid-work moves to the full loop, as PDR-002 requires — and every prose copy (AGENTS.md, the scaffold AGENTS.md, `CONTRIBUTING.md`, `guide/change-loop.md`, `guide/methodology.md`) follows that shape; the tier boundaries themselves are unchanged | Routing is the most frequent judgement an agent makes, so it has to be recallable from memory: one shape per tier with the protected-surface inventories as supporting detail, rather than three differently-shaped definitions buried in obligation lists. Wording is cheap and local to reverse, so no full record is warranted | CH-054 |
| 2026-07-23 | QS-001 and QS-002 are retired tombstones, never reused; their content is re-minted as constraints C-014 (coverage floor) and C-015 (onboarding), and the `quality` artifact type folds into the constraints register | The fold is decided in ADR-027 (a full record); this row is the ID tombstone, since the deleted `docs/quality/` files can no longer carry a `@retired` marker themselves — the retired IDs stay findable here and in `git log docs/quality/` | CH-053 / ADR-027 |
| 2026-07-23 | P-004 is closed `completed`: all three milestones (M-013, M-014, M-015) are `done` with evidence, and no successor plan is designated | The campaign's exit criteria are satisfied and evidenced by CH-046, CH-047, and CH-049; leaving the plan `active` after its work is finished would misstate what's in flight | CH-050 |
| 2026-07-22 | The first-try onboarding campaign improves the public value proposition, safe demo, release-binary installation, enforced-CI guidance, adoption fit, compatibility boundaries, and maintenance guidance without changing validator behavior or QS-002; installer scripts, package-manager channels, recordings, external-adoption claims, testimonials, and productivity measurements stay outside the campaign until supported by their own evidence and scope | Keeps the campaign focused on demonstrated documentation gaps without turning unverified adoption suggestions into product promises or distribution commitments | CH-045 / P-004 |
| 2026-07-21 | PDR-007 supersedes CH-023's “human review gate” interpretation: a ready PR is the mandatory authorization and protected-integration boundary, not a requirement for duplicate human code review; only a human-controlled PR merge accepts the agent's candidate, and hosted CI blocks acceptance only when its PR check is required by branch protection | Keeps the guardrail sharp without inventing review ceremony: the agent cannot accept its own work or silently skip protected CI, while a solo developer may accept the candidate locally before publication | CH-043 / PDR-007 |
| 2026-07-20 | Before an agent reports a change ready, every intended edit is committed, applicable local verification passes against that commit, the worktree is clean, the verified commit is pushed, a ready hosted PR exists, its head branch and SHA equal the locally verified branch and `HEAD`, and review updates repeat that local-before-publish sequence; a human-requested earlier stop is explicitly incomplete and not mergeable | A human and CI can review only hosted commits, so local or unpushed state cannot be part of the claimed ready change | CH-039 |
| 2026-07-19 | `clue validate` rejects a UTF-8 BOM anywhere in a corpus file and a complete second frontmatter block opening an artifact body; the `clue-extract` contract requires conversions to replace existing frontmatter and leave exactly one block | Invisible byte prefixes can hide fence boundaries, while duplicate blocks create competing metadata sources that the parser cannot reconcile safely | CH-031 / PR [#30](https://github.com/cliewen/cliewen/pull/30) |
| 2026-07-18 | Analysis records an evidence boundary: source revisions when possible, relevant reproduction conditions, and observed/inferred/unverified confidence; repository activity is not maintainer intent without explicit evidence | Keeps plausible interpretations and environment-specific results from becoming durable truth without their qualification; the carrier is `clue-analysis` | CH-025 / PR [#24](https://github.com/cliewen/cliewen/pull/24) |
| 2026-07-18 | PRs open ready for review, never as drafts; unfinished work stays on the branch until verification passes | The PR is the human review gate for a completed proposal, while draft status declares that the proposal is not ready for that gate; carriers are the PR-opening step of `clue-delta`, the extraction-PR contract of `clue-extract`, and the hosted-PR check of `clue-verify` | CH-023 / PR [#22](https://github.com/cliewen/cliewen/pull/22) |
| 2026-07-17 | The scaffolded constraints register is seeded with exactly the methodology conventions no versioned skill carries — today C-001 (markdown never hard-wrapped), self-sourced so a kept custom AGENTS.md leaves no dangling source; skill-carried rules are not duplicated into it | Skills are versioned carriers held by the drift lint, so registering copies would create a second carrier that drifts; a rule living only in the scaffolded AGENTS.md would be prose-only from day one, which is what the register exists to prevent. A template edit reverses this cheaply | CH-020 / PR [#19](https://github.com/cliewen/cliewen/pull/19) (review finding) |
| 2026-07-17 | AC-001 (30-minute onboarding) retired and split: the mechanical path became AC-002/AC-024/AC-025, the 30-minute human journey became QS-002 | The clock spans reading and installing — a human journey no focused test pair can verify; the granularity rule says split before testing, and re-minting ACs is cheap and local | CH-020 |
| 2026-07-17 | The init template tree lives at `internal/scaffold/templates/`, not the originally sketched `/.cliewen/templates` | The Go toolchain ignores dot-directories, putting them out of `go:embed`'s reach; moving a template folder later is a cheap local change (the embed decision itself is ADR-018) | CH-020 |
| 2026-07-17 | After a reported merge the agent orients before proceeding: it describes the plan's next step in plain language and asks the human to start it, or asks what to do next when the plan is empty | Agents treated "PR merged" as a go signal and silently started the next task, leaving the human without a picture of where the plan stands; carrier is `clue-delta` step 5 | CH-018 / PR [#17](https://github.com/cliewen/cliewen/pull/17) |
| 2026-07-16 | Litmus wording disambiguated: reversal cost is the sole routing test; "constrains future changes" means expensive to undo, not wide in reach | Two agent reviewers read the two phrasings as different tests and reached opposite routings for the same decision; the human directed that the reversal-cost reading takes precedence and the ambiguity be removed | CH-017 / PR [#16](https://github.com/cliewen/cliewen/pull/16) (review dispute) |
| 2026-07-16 | The digest is never a task in `tasks.md`; the digest precondition applies to the work only | A self-referential "digest" task can only be ticked falsely (before the deletion) or violate the precondition (unticked at deletion) — both misreadings have occurred; carrier is `clue-delta` step 4 | CH-017 / PR [#16](https://github.com/cliewen/cliewen/pull/16) (review finding) |
| 2026-07-12 | Default test-coverage gate: 80% total statement coverage, enforced in CI | A tripwire for code paths between the AC-covered ones — high enough to catch untested code landing, low enough not to invite meaning-free tests; total (not per-package) so thin entry points don't force contortions | demoted from ADR-004 by CH-010; carrier stays the CI workflow template |
| 2026-07-12 | Parse frontmatter with gopkg.in/yaml.v3 | A hand-rolled YAML subset would reject legitimate frontmatter with confusing errors, and the judge must never be wrong about form; one small pure-Go dependency doesn't threaten single-binary distribution | demoted from ADR-003 by CH-010 |
