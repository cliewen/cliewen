---
id: AN-019
type: analysis
status: active
links: [P-013, M-063, M-067, AN-018, PDR-029, PDR-033]
title: Post-trim audit of the shipped methodology carriers
---

# AN-019 — Post-trim audit of the shipped methodology carriers

## Evidence boundary

This is a reread of CH-132's candidate after the canonical skill sources, their generated copies, the repository hub, and the scaffolded hub were edited. It compares the candidate to AN-018's register and the human answers recorded after that spike. It does not revise AN-018, whose pinned register remains the evidence of M-062's earlier carrier population.

## Findings

The candidate removes the two withdrawn rules AN-018 identified: `clue-plan` no longer requires a closing distillation, and `clue-delta` no longer gives the uncheckable "Keep deltas small" advice. `clue-delta`'s spec-first pause now reports both the proposal and implementation status before asking whether to begin implementation and push the branch, as PDR-033 requires.

The generated `clue-verify` and `clue-extract` skills no longer use positional references that point to sections moved above their procedures. Focused generator guards hold those repaired references, the complete pause status, and every managed-skill row in both owned hubs.

The human-approved routing decision places detailed hosted-head and merge-history procedure only in the generated review-boundary fragment. Both hubs retain pre-corpus classification, the plain route's branch/PR/human-merge boundary, focused corpus reading, the red line, local conventions, and the skills table; their light and full routes enter `clue-delta` before procedure. PDR-012, PDR-016, PDR-021, and PDR-035 now name that placement rather than treating the duplicated hub text as an independent carrier.

## Unmet exit condition

AN-018 records three surviving rules with no live trace: F-DW-03, F-DW-05, and F-RB-09. Its answer deliberately routes their missing carriers to M-067, which is still `todo`. They remain live in the candidate, so M-063 cannot truthfully claim that no surviving rule-bearing statement is untraceable. This is a plan contradiction, not an implementation defect: completing M-063 requires either carrying M-067's work forward or narrowing M-063's exit criterion to the carrier defects it actually owns.

## Consumer

CH-132's open question Q-02 asks the human to resolve the M-063/M-067 boundary. The result determines whether this change can proceed to digest.
